
riot:
  api-key: "RGAPI-66a5011a-1a0c-4707-9960-b55bd0f6757a"

logging:
  level:
    root: info

spring:
  datasource:
    connection-init-sql: "SET NAMES utf8mb4"
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/matcher?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: "0000"
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        globally_quoted_identifiers: true
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect